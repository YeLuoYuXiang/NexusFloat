# 参与开发

感谢有兴趣参与。提 issue 或 PR 前，请先花两分钟看完下面几点。

## 提 issue

请说明：

- 手机型号、系统版本（例如「小米 15，澎湃 OS 3，Android 16」）
- LSPosed 版本，以及作用域是否勾选了「系统界面」
- 具体现象。能截图最好，涉及数值不对时请说明「期望看到什么、实际看到什么」

如果是 FPS 相关的问题，请先在 App 里打开「FPS 诊断」开关，把数值后面跟着的来源字母
（b / r / f / g / p / v / x）一起发出来，这个字母能直接说明当时走的是哪条取数通道。

涉及某个模块完全没数值时，附上 `su -c 'ls -l <节点路径>'` 的结果也有帮助。

## 提 PR

- 一个 PR 只做一件事，方便 review 与回滚
- 改完请确保 `./gradlew assembleDebug` 能过
- 提交信息写清楚「改了什么、为什么」，不用拘泥格式

## 代码约定

- 注释写「为什么这么写」，而不是复述代码在做什么。这类判断依据（某机型节点
  不写值、某个 API 从哪个版本起失效）是这个项目最值钱的部分，改动时请一并更新
- 涉及取数逻辑的改动，尽量说明是在什么机型、什么系统上验证的
- 新增取数来源时，注意「读不到」和「读到了 0」是两回事，不要混为一谈

## 不接受的改动

- 为了取数把 root shell 换成频繁 `cat` / `exec` 的写法。当前实现一轮采集在
  shell 内完成、零 fork，这是省电的基础（详见 README 的「取数开销」）
- 把 Choreographer 的刷新率当成应用帧率显示
- 引入统计、上报、广告之类的联网代码

## 许可

提交的代码会以 GPLv3 发布，请确认你同意这一点。
